// Faça um programa que leia vinte numeros inteiros e mostre o menor deles

// Resposta da pergunta
int controle=1;
    for(cont=1;cont<=20;cont++)
    {
        scanf("%d", &num);
        if (controle==1)
        {
        menor=num;
        }
        else
        if (num
        menor=num);
        controle++;
    }